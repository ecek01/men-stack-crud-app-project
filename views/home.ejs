<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to the Library App</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/partials.css">
</head>
<body class="home-page <%= user ? 'logged-in' : 'guest' %>">
  <header>

  </header>
  <main class="hero">
    <div class="hero-content">
      <h1 class="hero-title">Library App</h1>
      <p class="hero-subtitle">Your personal reading tracker.</p>
      <div class="button-group">
        <% if (user) { %>
          <a class="button button-library" href="/books">My Library</a>
          <a class="button button-account" href="/account">My Account</a>
          <a class="button button-logout" href="/auth/logout">Logout</a>
        <% } else { %>
          <a class="button button-signup" href="/auth/register">Register</a>
          <a class="button button-signin" href="/auth/login">Login</a>
        <% } %>
      </div>
    </div>
  </main>
</body>
</html>

